# NOC Tune - Requirements
# Network Quality Measurement Tool

# ============================================
# Core Dependencies
# ============================================

# Jupyter Notebook
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Progress Bar
tqdm>=4.65.0

# Excel Processing
openpyxl>=3.1.0

# Utilities
python-dateutil>=2.8.0
ipywidgets>=8.0.0

# ============================================
# Platform-Specific: WiFi Detection
# ============================================

# macOS Only - WiFi signal detection (RSSI, channel, band)
# Install with: pip install pyobjc-framework-CoreWLAN
# Skip on Windows/Linux (will fail to install)
pyobjc-framework-CoreWLAN>=10.0; sys_platform == 'darwin'

# Windows Only - WiFi detection (alternative)
# Note: pywifi is optional, netsh commands work without it
# pywifi>=1.1.12; sys_platform == 'win32'

# Linux Only - WiFi detection (alternative)
# Note: Usually iwconfig/nmcli are available by default
# wireless-tools is a system package, not pip

# ============================================
# Optional: Browser-based Geolocation
# ============================================
# For get_location.py script (precise GPS via browser)
# These are in Python standard library, no extra install needed:
# - http.server
# - webbrowser
# - json
# - threading

# ============================================
# Optional: Advanced Network Analysis
# ============================================
# dnspython>=2.4.0
# scapy>=2.5.0
